name: Keep Streamlit demo awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping demo URL
        run: curl -sSf --max-time 30 https://himanshumjain15-recsys.streamlit.app/ -o /dev/null
