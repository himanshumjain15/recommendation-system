# Local context — never push
CLAUDE.md
.claude/

# Environment / secrets
.env
.venv/
venv/

# Raw data — ml-latest-small is committed (see sample_data/inputs/ml-latest-small)
# since it's small and downloading it at runtime depends on GroupLens's uptime.
# Anything else placed in sample_data/inputs (e.g. a larger MovieLens variant) stays ignored.
sample_data/inputs/*
!sample_data/inputs/ml-latest-small/
sample_data/outputs/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
